apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-210305-90zq-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-210305-90zq.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-210305-90zq-service
            port:
              number: 80
